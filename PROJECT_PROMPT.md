# برومبت تطوير: تطبيق إسلامي شامل ومفتوح المصدر لنظام أندرويد

> **الاسم الرسمي:** **Muslim (مسلم)** — موحّد للمشروع والحزمة (`org.muslim.app`) والتطبيق؛ اسم التطبيق مُترجم حسب لغة العرض لكل لغات العالم (185 لغة)
> **الوصف بجملة واحدة:** تطبيق أندرويد إسلامي شامل، مجاني بالكامل ومفتوح المصدر، يبدأ بالأذان وأوقات الصلاة والقبلة بدقة فلكية عالية، ويتوسّع تدريجيًا ليشمل القرآن الكريم، الأحاديث، الأذكار، رمضان، حاسبة الزكاة، وكل ما يحتاجه المسلم يوميًا.
> **المنصة:** Android (Kotlin + Jetpack Compose)
> **الترخيص المقترح:** مفتوح المصدر (GPLv3 — انظر القسم 9)
> **معرّف الحزمة المؤقت:** `org.muslim.app` (يُستبدل عند التسجيل الفعلي)

## عن هذا المستند

هذا مستند "برومبت" (Prompt) تفصيلي مُعدّ لتوجيه بناء التطبيق الموصوف أعلاه. صُمم ليُستخدم كسياق مشروع (Project Prompt) يُلصَق كاملاً في بداية جلسة مع أداة ذكاء اصطناعي مساعدة في البرمجة (مثل Claude Code)، أو كمرجع تأسيسي لفريق تطوير بشري. عامله كمرجع حي: حدّثه كلما تغيّر قرار مهم، واجعله "مصدر الحقيقة الوحيد" للمشروع.

**فهرس المحتويات:**
1. دورك في هذا المشروع
2. الرؤية والمبادئ الأساسية
3. البنية التقنية (Architecture)
4. نظام التصميم (Material Design 3 Expressive)
5. التدويل ودعم اللغات
6. خارطة الطريق التفصيلية للميزات (8 مراحل)
7. متطلبات غير وظيفية
8. الخصوصية والأمان
9. الترخيص والمساهمة المجتمعية
10. معايير الدقة والمحتوى الشرعي
11. كيفية استخدام هذا البرومبت عمليًا
12. ملحق: مراجع تقنية مقترحة

---

## 1. دورك في هذا المشروع

أنت مهندس/ة برمجيات أندرويد خبير، متخصص في:
- Kotlin وJetpack Compose وأحدث ممارسات تطوير أندرويد (2025–2026).
- بنية Clean Architecture متعددة الوحدات (Multi-module).
- الحسابات الفلكية الدقيقة لتحديد أوقات الصلاة واتجاه القبلة.
- نظام تصميم Material Design 3 Expressive، وإمكانية الوصول، والتدويل.

**مهمتك:** بناء التطبيق الموصوف في هذا المستند مرحلةً تلو الأخرى، بدءًا بالأذان كأساس (MVP)، مع الحفاظ في كل خطوة على: بنية قابلة للتوسّع، كود نظيف وموثّق، اختبارات كافية، وتصميم متسق يتبع أحدث إرشادات Google. لا تنتقل إلى مرحلة جديدة قبل أن تعمل المرحلة الحالية بجودة كاملة ومستقرة.

## 2. الرؤية والمبادئ التي لا تقبل التنازل

- **مجاني للأبد، بلا إعلانات وبلا اشتراكات:** التطبيق خدمة للمسلمين لا منتج تجاري. لا SDK إعلانات، لا "شراء داخل التطبيق" لفتح ميزات أساسية.
- **مفتوح المصدر بالكامل:** الكود منشور علنًا، والمساهمة المجتمعية (كودًا وترجمة ومحتوى) جزء أساسي من استراتيجية النمو.
- **الخصوصية أولًا:** لا تتبّع، لا جمع بيانات غير ضروري، لا أذونات لا يحتاجها التطبيق فعليًا. البيانات تبقى على الجهاز افتراضيًا.
- **الدقة أولًا:** خصوصًا في أوقات الصلاة، اتجاه القبلة، والتقويم الهجري — أي خطأ هنا له أثر ديني مباشر على المستخدم.
- **يعمل بلا إنترنت (Offline-first):** كل الوظائف الأساسية (الأذان، القبلة، الأذكار، نصوص القرآن) تعمل بدون اتصال بعد الإعداد الأول.
- **شامل للجميع:** دعم كل لغات العالم، ودعم المذاهب الفقهية المختلفة عبر إعدادات قابلة للتخصيص بدل فرض مذهب واحد.
- **قابل للتوسّع والصيانة:** كل ميزة جديدة تُضاف كوحدة (module) مستقلة لا تكسر ما سبقها.
- **إتاحة كاملة (Accessibility):** دعم قارئ الشاشة، تكبير الخط، وتباين الألوان لذوي الاحتياجات الخاصة.

## 3. البنية التقنية (Technical Architecture)

### 3.1 لغة البرمجة وواجهة المستخدم

| العنصر | الاختيار المقترح |
|---|---|
| لغة البرمجة | Kotlin 100% (بدون Java) |
| واجهة المستخدم | Jetpack Compose بالكامل (بدون XML Views) |
| نظام التصميم | Material Design 3 Expressive (أحدث تطور لنظام تصميم Google) |
| الحد الأدنى لإصدار أندرويد | **API 26 / Android 8** (تم التثبيت عند بدء التنفيذ) |
| الإصدار المستهدف (target/compile SDK) | **Android 17 "Cinnamon Bun" (API 37)** — compileSdk وtargetSdk = 37 |

### 3.2 نمط البنية المعمارية

- **Clean Architecture** بثلاث طبقات: `presentation` (Compose + ViewModel) ← `domain` (Use Cases + نماذج نقية) ← `data` (Repositories + مصادر البيانات المحلية/البعيدة).
- **MVI/MVVM** على مستوى الشاشة: حالة واحدة غير قابلة للتغيير (Immutable UI State) وتدفق أحداث عبر `StateFlow` / `SharedFlow`.
- **حقن التبعيات:** Hilt.
- **البرمجة اللامتزامنة:** Kotlin Coroutines + Flow في كل مكان (بدون Callbacks أو RxJava).

### 3.3 تقسيم المشروع إلى وحدات (Modularization)

بنية متعددة الوحدات منذ اليوم الأول لضمان قابلية التوسّع والصيانة:

```
app/                          # نقطة الدخول + التنقل الرئيسي
core/
  ├── core-ui/                 # مكونات Compose مشتركة + الثيم
  ├── core-design-system/      # الألوان، الطباعة، الأيقونات، الحركة
  ├── core-common/              # أدوات عامة وامتدادات Kotlin
  ├── core-database/           # Room + DAOs المشتركة
  ├── core-datastore/          # تفضيلات المستخدم (DataStore)
  ├── core-network/            # Retrofit/OkHttp المشترك (اختياري)
  ├── core-notifications/      # نظام إشعارات موحّد (يشمل Bubbles)
  └── core-location/           # GPS + الحساسات (البوصلة)
feature/
  ├── feature-prayer-times/     # المرحلة 1
  ├── feature-qibla/            # المرحلة 1
  ├── feature-quran/            # المرحلة 2
  ├── feature-hadith/           # المرحلة 3
  ├── feature-adhkar/           # المرحلة 4
  ├── feature-tasbih/           # المرحلة 4
  ├── feature-learn/            # المرحلة 5
  ├── feature-ramadan/          # المرحلة 6
  ├── feature-zakat/            # المرحلة 7
  └── feature-settings/         # عابرة لكل المراحل
```

كل `feature module` يعتمد على وحدات `core` فقط، ولا يعتمد مباشرة على وحدة feature أخرى (أي تواصل بينها يمر عبر واجهات في `core` أو طبقة التنقّل).

### 3.4 قاعدة البيانات والتخزين

- **Room** للبيانات البنيوية (نصوص القرآن، الأذكار، الأحاديث، سجلات الزكاة...) تُشحن كقاعدة بيانات مُعدّة مسبقًا (pre-populated database) لضمان العمل الكامل بدون إنترنت من أول تشغيل.
- **DataStore** (Preferences + Proto) لتفضيلات المستخدم.
- تنزيلات اختيارية (تلاوات صوتية، تفاسير إضافية) في التخزين الداخلي، مع إدارة مساحة واضحة يراها المستخدم ويتحكم بها.

### 3.5 الجدولة والتنبيهات في الخلفية (حرجة لدقة الأذان)

- `AlarmManager` مع `setExactAndAllowWhileIdle` والتحقق من `canScheduleExactAlarms` لضمان انطلاق الأذان في وقته تمامًا حتى في وضع Doze.
- إعادة جدولة تلقائية عند إعادة الإقلاع (`BOOT_COMPLETED`) وعند تغيّر الموقع أو المنطقة الزمنية.
- شاشة توجيه (onboarding) شفافة تشرح للمستخدم أهمية استثناء التطبيق من تحسين البطارية، دون إجبار.
- `WorkManager` للمهام غير الحرجة زمنيًا (تنزيلات، مزامنة تفضيلات).
- خدمة أمامية خفيفة واختيارية لإشعار الأذكار الدوري (تفصيل تقني في القسم 6، المرحلة 4).

### 3.6 الشبكة (اختيارية بالكامل)

التطبيق يعمل 100% بدون إنترنت. الاتصال بالشبكة يُستخدم فقط لميزات إضافية اختيارية:
- تنزيل تلاوات صوتية إضافية للقرآن.
- تنزيل تفاسير/ترجمات إضافية.
- تحديث سعر الذهب/الفضة لحاسبة الزكاة (مع إبقاء خيار الإدخال اليدوي متاحًا دائمًا).
- الأدوات: Retrofit + OkHttp + kotlinx.serialization.

### 3.7 الاختبارات

- اختبارات وحدة لمحرك حساب أوقات الصلاة، بمقارنة النتائج مع جداول مرجعية رسمية (دقة الثواني).
- اختبارات وحدة لحساب اتجاه القبلة (Great-circle bearing).
- اختبارات Compose UI للشاشات الحرجة.
- اختبار الترجمة الزائفة (Pseudolocalization) للتأكد من سلامة التخطيط مع النصوص الطويلة ومع RTL.

### 3.8 أدوات ومعايير عامة

- Detekt / ktlint لتنسيق وجودة الكود.
- GitHub Actions لـ CI/CD (بناء، اختبار، فحص جودة عند كل Pull Request).
- توثيق KDoc للدوال والوحدات العامة.

## 4. نظام التصميم (Material Design 3 Expressive — أحدث معايير Google)

> **ملاحظة سياقية:** أحدث نظام تصميم أطلقته Google هو **Material 3 Expressive**، الذي أُعلن في Google I/O 2025 ولا يزال يتدرّج عبر تطبيقات Google طوال 2026 (حاويات بصرية جديدة، أشكال قابلة للتحوّل، حركة "نابضة" أكثر طبيعية). ابنِ عليه مباشرة بدل الاعتماد على إصدارات Material أقدم.

### 4.1 الألوان والثيمات

- دعم **Dynamic Color** (Material You) المُستخرج من خلفية جهاز المستخدم على Android 12 فأعلى، مع لوحة ألوان احتياطية مصممة يدويًا للإصدارات الأقدم.
- ثلاثة أوضاع: **فاتح**، **داكن**، **حسب النظام** — قابلة للتبديل الفوري بدون إعادة تشغيل، بانتقال بصري سلس.
- تباين ألوان يحقق معايير WCAG AA كحد أدنى في كل الأوضاع.

### 4.2 الطباعة (Typography)

- نظام طباعة Material 3 الكامل (Display/Headline/Title/Body/Label بأحجامها الخمسة).
- خط عربي عالي الجودة وقابل للقراءة (مثل Noto Naskh Arabic أو IBM Plex Sans Arabic) بجانب خط لاتيني متوافق (Roboto Flex / Google Sans).
- حجم خط ديناميكي يستجيب لإعدادات إتاحة النظام (Font Scale) حتى 200% دون كسر التخطيط.
- خط عثماني متخصص لنص القرآن (مثل خطوط مجمع الملك فهد / KFGQPC) منفصل عن خط واجهة التطبيق.
- استخدام Material Symbols (نظام الأيقونات الحديث القابل للتخصيص كـ variable font) بدل Material Icons القديمة.

### 4.3 الحركة والانتقالات (Motion)

- نظام حركة Material 3 Expressive: حركة "نابضة" (spring-based) طبيعية بدل المنحنيات الثابتة القديمة.
- انتقالات عناصر مشتركة (Shared Element Transitions) بين الشاشات ذات الصلة (مثلاً من قائمة السور إلى شاشة القراءة).
- حركة هادئة وغير مبالغ فيها في السياقات التعبدية (تسبيح، قراءة قرآن) — الأولوية للتركيز لا للترفيه البصري.

### 4.4 المكونات والتخطيطات المتكيفة (Adaptive Layouts)

- استخدام `WindowSizeClass` لدعم الهواتف والأجهزة القابلة للطي والأجهزة اللوحية بتخطيطات متكيفة (قائمة وتفاصيل جنبًا إلى جنب على الشاشات الكبيرة)، بما يتماشى مع توجّه أندرويد الحديث نحو التطوير "التكيّفي أولًا" (Adaptive-first).
- شريط تنقل سفلي للهواتف، يتحول إلى Navigation Rail أو Drawer على الشاشات الأوسع.
- دعم كامل لتخطيطات RTL وLTR بالتبديل التلقائي حسب لغة الواجهة.

### 4.5 الهوية البصرية

- زخارف هندسية إسلامية بسيطة ومحترمة (لا تصويرية) كعناصر اختيارية في الخلفيات/الشاشات الترحيبية، دون المساس بوضوح Material 3.
- أيقونة تطبيق تدعم Adaptive Icons وThemed Icons (Material You).

### 4.6 إمكانية الوصول (Accessibility)

- تسميات محتوى كاملة (Content Descriptions) لكل عنصر تفاعلي، لدعم TalkBack.
- أهداف لمس لا تقل عن 48dp.
- وضع "قراءة مريحة" اختياري (تباين أعلى، تباعد أسطر أكبر) لنص القرآن والأذكار.
- دعم تحويل نص إلى صوت (TTS) كبديل للصوت المسجل عند عدم توفره باللغة المطلوبة.

## 5. التدويل ودعم اللغات (Internationalization)

- بنية موارد نصوص تدعم من اليوم الأول **كل لغات العالم** دون قيد تقني، بدءًا بالعربية، الإنجليزية، الأردية، الإندونيسية، الملايو، التركية، الفارسية، الفرنسية، الألمانية، الإسبانية، الروسية، البنغالية، الهاوسا، السواحيلية، والصينية — وتُضاف لغات أخرى تدريجيًا عبر مساهمات مجتمعية.
- فصل كامل بين نصوص الواجهة (قابلة للترجمة بسهولة) ونصوص المحتوى الديني (القرآن/الأذكار/الأحاديث) التي تُدار كبيانات منفصلة موثوقة المصدر.
- منصة ترجمة مجتمعية مفتوحة (مثل Weblate أو Crowdin) لتسهيل مساهمة المتطوعين بدون الحاجة لمعرفة برمجية.
- تنسيق التواريخ والأرقام حسب اللغة (أرقام هندية/عربية حسب تفضيل المستخدم، عرض التقويم الهجري والميلادي معًا).
- إمكانية اختيار لغة التطبيق بشكل مستقل عن لغة نظام الجهاز.

## 6. خارطة الطريق التفصيلية للميزات

> كل مرحلة وحدة (feature module) مستقلة قابلة للتسليم والاختبار بمفردها قبل الانتقال للتالية. ابدأ دائمًا بالمرحلة 1 وأنجزها بجودة كاملة أولًا.

### المرحلة 1 (الأساس / MVP): الأذان، أوقات الصلاة، القبلة، والتقويم الهجري

**محرك حساب أوقات الصلاة**
- [x] حساب فلكي دقيق (زاوية ميل الشمس، معادلة الزمن) بدقة الثانية — لا الاعتماد على API خارجي كمصدر وحيد للحساب. ✅ (منفّذ ومُتحقق منه مقابل متجهات Adhan المرجعية — 16 اختبار وحدة)
- [x] دعم طرق الحساب العالمية: رابطة العالم الإسلامي، ISNA، الهيئة المصرية، أم القرى، كراتشي، طهران، الجعفري، الكويت، قطر، سنغافورة، فرنسا، ديانت تركيا، لجنة رؤية الهلال، دبي، وإعداد مخصّص بزاويتي الفجر/العشاء.
- [x] اختيار مذهب حساب العصر (الجمهور/الحنفي).
- [x] معالجة خطوط العرض العالية (Higher Latitude Rules: طريقة الزاوية، منتصف الليل، السُبع الأخير).
- [x] تعديل يدوي دقيق (بالدقيقة) لكل صلاة على حدة، محفوظ كتفضيل دائم.
- [x] تحديد الموقع تلقائيًا (GPS عبر FusedLocation) + بحث يدوي عن مدينة (قاعدة بيانات مدن مدمجة تعمل دون إنترنت) + إدخال إحداثيات يدوي.
- [x] جدول صلوات يومي وشهري، مع مشاركة/تصدير اليوم كملف نصي عبر Share Sheet (الطباعة لاحقًا).
- [x] عدّاد تنازلي مباشر للصلاة القادمة في الشاشة الرئيسية والـ Widget.

**اتجاه القبلة**
- [x] حساب الاتجاه الجغرافي الدقيق نحو الكعبة (21.4225°N، 39.8262°E) عبر معادلة الـ Great-circle bearing (8 اختبارات وحدة).
- [x] بوصلة تدمج بيانات عدة حساسات (مقياس مغناطيسي + متسارع عبر مصفوفة الدوران؛ دمج الجيروسكوب لاحقًا) لتقليل الاهتزاز والتشويش.
- [x] تصحيح الانحراف المغناطيسي (Magnetic Declination) تلقائيًا حسب الموقع.
- [x] واجهة معايرة واضحة عند اكتشاف تشويش في الحساسات.
- [x] عرض بديل على خريطة (خط مستقيم من موقع المستخدم إلى مكة).
- [x] عرض المسافة إلى مكة المكرمة.

**الأذان والإشعارات**
- [x] مكتبة أصوات أذان متعددة من دول مختلفة — قابلة للتنزيل (صوت مدمج + مكتبة تنزيل اختيارية عبر `core-network`؛ النموذج يدعم `soundId` لكل صلاة).
- [x] اختيار سلوك أذان لكل صلاة على حدة (الأذان الافتراضي / اهتزاز فقط / صامت) محفوظ في الإعدادات — والنموذج جاهز لربط مكتبة أصوات قابلة للتنزيل (soundId) لاحقًا.
- [x] تحكم بمستوى الصوت (0–100%) + تأثير Fade-in تدريجي (4 ثوانٍ) + زر معاينة في الإعدادات.
- [x] موثوقية تسليم الأذان في الخلفية: AlarmManager دقيق (مع رجوع تلقائي عند منع الأذان الدقيق) + خدمة أمامية + إعادة جدولة عند الإقلاع وتغيّر المنطقة الزمنية + بطاقة توجيه شفافة لاستثناء البطارية (غير مفروضة).
- [x] إشعار تذكير قبل الصلاة بوقت قابل للتخصيص (0/5/10/15/30 دقيقة).
- [x] Widgets متعددة الأحجام لشاشة الهاتف الرئيسية (Glance — 2×2/3×3/4×4: الصلاة القادمة + عدّاد تنازلي + مواقيت اليوم، تحديث كل 15 دقيقة وعند كل صلاة/إقلاع/تغيير الإعدادات؛ انظر `feature-prayer-times/widget/`).
- [x] اختصارات تطبيق سريعة (App Shortcuts) بالضغط المطوّل على الأيقونة: مواقيت الصلاة، القبلة، الإعدادات (توجيه عبر `muslim://` deep links).

**التقويم الهجري (ميزة أساسية عابرة لكل التطبيق)**
- [x] عرض التاريخ الهجري بجانب الميلادي في الشاشة الرئيسية وقائمة الصلوات (خوارزمية أم القرى، مبنية على java.time.chrono.HijrahChronology — 6 اختبارات).
- [x] تعديل يدوي للتاريخ الهجري (±يوم) على مستوى التطبيق كله (طريقة الحساب حاليًا: حسابية فلكية أم القرى؛ اتّباع إعلان رؤية هلال جهة محلية لاحقًا).
- [x] هذا الإعداد يُستخدم تلقائيًا لتحديد بداية/نهاية رمضان (المرحلة 6)، مع إمكان التجاوز اليدوي دائمًا.

### المرحلة 2: القرآن الكريم (صوت وصورة كامل)

- [x] النص الكامل بالرسم العثماني (6236 آية + بيانات السور) في قاعدة بيانات Room مملوءة مسبقًا من ملفات مضمّنة — تعمل دون إنترنت من أول تشغيل. (الرسم الإملائي لاحقًا)
- [x] مكتبة قرّاء متعددة عالميًا، تُنزَّل عند الطلب (سورة سورة أو ختمة كاملة) لتوفير المساحة.
- [x] تتبّع صوتي متزامن مع النص (تظليل الآية أثناء القراءة).
- [x] وضع "التكرار" لحفظ آية أو مقطع بعدد مرات محدد.
- [x] ترجمات معاني متعددة اللغات (تُنزَّل عند الطلب، تُعرض مع الآية).
- [x] تفاسير متعددة (كابن كثير، السعدي، الميسّر) تُنزَّل عند الطلب وتُعرض جنبًا إلى جنب مع الآية.
- [ ] ترجمة كلمة بكلمة اختيارية، مع تلوين أحكام التجويد اختياريًا.
- [x] بحث نصي في القرآن (فهرس FTS4 على النص المطبَّع، بحث فوري دون إنترنت، نتائج تقفز للآية في القارئ) — البحث في الترجمات/التفاسير عند توفرها.
- [x] علامات مرجعية (حفظ/إزالة لكل آية + شاشة علامات) ومتابعة آخر قراءة تلقائيًا (استئناف من موضع التوقف). (تتبّع تقدّم الختمة وخطة القراءة لاحقًا)
- [x] إشعار "آية اليوم" اختياري (WorkManager + إشعارات).
- [x] حجم خط قابل للتعديل في القارئ + وضع قراءة ليلي و"قراءة مريحة".

### المرحلة 3: الأحاديث النبوية الشريفة

- [x] مكتبة نصية (عيّنة مُصدَّرة تعمل دون إنترنت: أطراف/أمثلة من الكتب الستة والأربعين النووية ورياض الصالحين — استيراد الكتب الكاملة لاحقًا عبر أداة توليد + مراجعة شرعية).
- [x] تصنيف حسب المجموعة (تجميعات) لتسهيل التصفح — التصنيف الموضوعي الكامل لاحقًا.
- [x] مجموعات مختصرة شهيرة كمدخل سهل: الأربعون النووية، رياض الصالحين (عينات).
- [x] عرض درجة الحديث (صحيح/حسن/ضعيف) مع الإشارة للمصدر.
- [x] بحث نصي FTS في متن الأحاديث.
- [ ] إشعار "حديث اليوم" اختياري (الحديث اليومي معروض داخل الشاشة حاليًا).
- [x] علامات مرجعية ومشاركة الحديث كنص (مشاركة صورة مصممة لاحقًا).

### المرحلة 4: الأذكار والتسبيح

- [x] مجموعات أذكار شاملة وموثّقة المصدر (44 ذكرًا: الصباح، المساء، بعد الصلاة، النوم، الاستيقاظ، الطعام، السفر، وأدعية عامة) مبنية على "حصن المسلم" وأمهات كتب السنة مع ذكر التخريج لكل ذكر.
- [x] عدّاد لكل ذكر يتوقف عند التكرار المطلوب، محفوظ بين الجلسات.
- [x] مسبحة إلكترونية بتغذية راجعة اهتزازية، هدف قابل للتخصيص (33/99/100/1000)، سجل يومي (آخر 30 يومًا برسم بياني)، وإمكانية إعادة الضبط.
- [x] Widget للمسبحة على الشاشة الرئيسية (يعرض اليوم وهدفك ويعدّ بلمسة).
- [ ] **إشعار عائم قابل للتخصيص للأذكار:** تذكير دوري (كل مدة يحدّدها المستخدم بالدقائق/الساعات) بذكر عشوائي أو مُختار، مع إمكانية إيقافه بالكامل أو جدولته لفترات محددة فقط (مثلاً أوقات الدوام فقط).
  - **التنفيذ التقني المقترح (2026):** استخدام Notification Bubbles (`BubbleMetadata`، متاحة منذ Android 11) لعرض التذكير كفقاعة صغيرة عائمة قابلة للنقر تفتح نافذة مصغّرة بالذكر ثم تُطوى تلقائيًا — وهي الآلية الرسمية المخصصة لهذا النوع من التذكيرات الخفيفة. يمكن الاستفادة اختياريًا أيضًا من ميزة **App Bubbles** الجديدة في نظام النوافذ بأندرويد 17 (نظام منفصل تقنيًا رغم تشابه الاسم) للسماح للمستخدم بإبقاء نافذة مصغّرة للتسبيح عائمة فوق التطبيقات الأخرى. مع رجوع (fallback) لإشعار Heads-up تقليدي على الإصدارات الأقدم من Android 11.

### المرحلة 5: التعلّم (الصلاة، الوضوء، وكل الصلوات)

- [x] دليل الوضوء خطوة بخطوة (نص مرتّب؛ الرسوم التوضيحية والفيديو لاحقًا).
- [x] دليل الغسل والتيمم.
- [x] دليل الصلوات الخمس اليومية: عدد الركعات، والأذكار داخل كل ركن، بترتيب تفاعلي يمكن متابعته أثناء الصلاة.
- [x] عرض الفروقات المذهبية بحياد (حنفي/شافعي/مالكي/حنبلي) دون ترجيح.
- [x] صلوات خاصة: صلاة الجنازة، صلاة العيدين، التراويح، الوتر، الاستخارة، الضحى، التهجّد، والجمع والقصر للمسافر.
- [x] جدول مرجعي سريع لعدد ركعات كل صلاة.
- [ ] وضع مبسّط للأطفال والمبتدئين (اختياري).

### المرحلة 6: رمضان

- [x] وضع رمضان: عدّاد تنازلي للسحور والإفطار، مع تنبيه سحور قابل للتخصيص (15/30/45/60 دقيقة قبل الفجر) عبر أذان دقيقة.
- [x] إشعار دخول وقت الإفطار مقترنًا بدعاء الإفطار.
- [x] تقويم رمضان (حساب أم القرى + تطبيق تعديل الهجري) مع متابعة أيام الصيام (شبكة 30 يومًا قابلة للتسجيل).
- [ ] تنبيهات العشر الأواخر والليالي الوترية المرجّحة لليلة القدر.
- [ ] استخدام إعدادات التقويم الهجري من القسم الأساسي (المرحلة 1) لتحديد بداية/نهاية رمضان والعيدين تلقائيًا، مع بقاء إمكانية التجاوز اليدوي دائمًا متاحة.
- [ ] تذكير زكاة الفطر قبل العيد بفترة قابلة للتخصيص.

### المرحلة 7: حاسبة الزكاة

- [x] حاسبة زكاة المال (النقد، الذهب، الفضة، عروض التجارة، الاستثمارات) بعد خصم الديون، مع مقارنة نصابي الذهب (85غ) والفضة (595غ).
- [ ] نصاب يُحدَّث تلقائيًا حسب سعر الذهب/الفضة العالمي — متاح يدويًا الآن (Offline-first)؛ التحديث الشبكي لاحقًا.
- [x] حاسبة زكاة الفطر (حسب عدد أفراد الأسرة والقيمة/الصاع المحلي).
- [ ] دعم عملات متعددة (الحساب متعادل العملة حاليًا؛ اختيار عملة واجهة لاحقًا).
- [x] حفظ سجل حسابات الزكاة (آخر 50 حسابًا).
- [x] شرح مبسّط للأحكام.

### المرحلة 8: ميزات إضافية موصى بها (لإكمال الاحتياج الشامل للمسلم)

- [ ] **أسماء الله الحسنى:** قائمة تفاعلية مع الشرح والصوت.
- [ ] **مكتبة أدعية موسّعة:** أدعية قرآنية ونبوية مصنّفة حسب المناسبة (سفر، مرض، همّ وحزن، زواج، مولود، استخارة...).
- [ ] **التقويم والمناسبات الإسلامية:** الأعياد، عاشوراء، المولد النبوي، أيام الحج، مع تذكيرات مسبقة.
- [ ] **الصيام المسنون:** تذكير بالاثنين والخميس، الأيام البيض، يوم عرفة، عاشوراء.
- [ ] **دليل الحج والعمرة:** خطوات المناسك تفصيليًا (مرحلة مستقبلية، قد تشمل خرائط تفاعلية).
- [ ] **باحث المساجد القريبة:** عرض المساجد على خريطة (عبر بيانات مفتوحة مثل OpenStreetMap لتفادي التبعية الكاملة لخدمات مدفوعة).
- [ ] **ملفات متعددة للأسرة:** دعم مواقع/تفضيلات مختلفة لأفراد الأسرة على نفس الجهاز.
- [ ] **نسخ احتياطي واستعادة:** تصدير/استيراد الإعدادات والتقدّم الشخصي كملف محلي، مع خيار مزامنة عبر حساب Google Drive الخاص بالمستخدم نفسه (اختياري بالكامل، وليس عبر خوادم التطبيق).
- [ ] **دعم Wear OS** لعرض الصلاة القادمة والتسبيح على الساعات الذكية (مرحلة مستقبلية).

### وحدة الإعدادات العامة (تُبنى تدريجيًا مع كل مرحلة)

مركز تحكم واحد يجمع كل خيارات التخصيص المذكورة أعلاه، منظّم في أقسام واضحة:

- **الصلاة والأذان:** طريقة الحساب، المذهب، التعديل اليدوي لكل صلاة، صوت الأذان لكل صلاة.
- **القبلة:** وحدة قياس المسافة، خيار عرض الخريطة كبديل دائم.
- **القرآن:** القارئ الافتراضي، الترجمة والتفسير الافتراضيان، حجم الخط.
- **الأذكار:** تفعيل/تعطيل الإشعار الدوري، ضبط الفاصل الزمني، اختيار فئات الأذكار المفضّلة.
- **المظهر:** فاتح/داكن/حسب النظام، حجم الخط، إيقاف الحركات (لتفضيلات إتاحة الوصول).
- **اللغة:** لغة الواجهة (مستقلة عن لغة الجهاز)، لغة التلاوة والترجمة.
- **الإشعارات:** تحكم دقيق بكل نوع إشعار على حدة (تفعيل/تعطيل/توقيت).
- **البيانات والخصوصية:** نسخ احتياطي محلي، تصدير البيانات، مسح البيانات.
- **حول التطبيق:** الترخيص، رابط الكود المصدري، طرق المساهمة.

## 7. متطلبات غير وظيفية (Non-Functional Requirements)

- **الأداء:** زمن إقلاع أقل من ثانيتين، حجم تثبيت أساسي صغير (المحتوى الثقيل كالتلاوات الصوتية يُنزَّل عند الطلب).
- **استهلاك البطارية:** قياس وتحسين مستمر لتأثير التنبيهات المجدولة والحساسات — البوصلة تعمل فقط عند فتح شاشة القبلة، لا في الخلفية.
- **الاستقرار:** معالجة شاملة للأخطاء، خصوصًا حول الأذونات المرفوضة (الموقع، الإشعارات) — يبقى التطبيق مفيدًا جزئيًا حتى لو رفض المستخدم إذنًا معينًا.
- **قابلية التوسّع:** تصميم يدعم ملايين المستخدمين حول العالم دون أي بنية خادم مركزية إلزامية.

## 8. الخصوصية والأمان

- عدم دمج أي SDK إعلانات أو تتبّع تسويقي.
- طلب الأذونات فقط عند الحاجة الفعلية للميزة (Permission just-in-time) مع شرح واضح لسبب الطلب.
- بيانات الموقع تُستخدم محليًا لحساب الأوقات فقط، ولا تُرسَل لأي خادم خارجي.
- سياسة خصوصية واضحة ومختصرة، منشورة داخل التطبيق وفي مستودع الكود.

## 9. الترخيص والمساهمة المجتمعية

- **رخصة مقترحة:** GPLv3 (تضمن بقاء أي نسخة معدَّلة من التطبيق مفتوحة المصدر أيضًا)، أو MIT/Apache 2.0 إن كانت الأولوية لأوسع انتشار واستخدام حر — القرار يعتمد على فلسفة الفريق المؤسس.
- ملفات أساسية: `README.md` (شرح المشروع وخطوات البناء)، `CONTRIBUTING.md` (كيفية المساهمة بالكود/الترجمة/المحتوى الديني)، `CODE_OF_CONDUCT.md`.
- قناة تواصل للمساهمين (Discord/Telegram/GitHub Discussions).
- نظام مراجعة خاص للمحتوى الديني منفصل عن مراجعة الكود (انظر القسم التالي).

## 10. معايير الدقة والمحتوى الشرعي (تنويه هام)

كل محتوى ديني في التطبيق (نص القرآن، الأحاديث ودرجتها، الأذكار والأدعية، خطوات الصلاة والوضوء، أحكام الزكاة) يجب أن:

- يُستقى من مصادر موثوقة ومعتمدة (مثل مجمع الملك فهد لطباعة المصحف الشريف لنص القرآن، ومصادر تخريج معروفة للأحاديث).
- يُراجَع من قِبل مختصين/طلبة علم شرعي قبل النشر النهائي — **هذا المستند مرجع تقني وهيكلي، وليس بديلاً عن المراجعة الشرعية المتخصصة.**
- يعرض الخلافات الفقهية بحياد دون ترجيح مذهب على آخر، إلا إذا اختار المستخدم ذلك صراحة في الإعدادات.
- محرك حساب أوقات الصلاة يُختبر ويُقارن بجداول مواقيت رسمية معتمدة (كمواقيت الهيئات الحكومية في عدة دول) قبل الإطلاق.

## 11. كيفية استخدام هذا البرومبت عمليًا

1. ابدأ محادثة جديدة مع أداة الذكاء الاصطناعي المساعدة في البرمجة (مثل Claude Code)، والصق هذا الملف كاملاً كسياق أولي للمشروع.
2. اطلب أولًا توليد هيكل المشروع (المجلدات والوحدات) المذكور في القسم 3.3 فقط، دون كتابة منطق الميزات بعد. ✅ **تم تنفيذه (هيكل المشروع متعدد الوحدات جاهز ويُبنى بنجاح).**
3. انتقل لتنفيذ المرحلة 1 فقط (الأذان + القبلة + التقويم الهجري) بالكامل: المنطق، الواجهة، الاختبارات.
4. لا تنتقل لمرحلة جديدة قبل أن تعمل المرحلة الحالية بشكل مستقر وتُختبر يدويًا على جهاز حقيقي.
5. حدّث هذا الملف نفسه كلما تغيّر قرار معماري أو تصميمي مهم، ليبقى مصدر الحقيقة الوحيد للمشروع.

## 12. ملحق: مراجع تقنية مقترحة (نقطة انطلاق للبحث، وليست قائمة نهائية)

- **خوارزميات أوقات الصلاة:** خوارزميات PrayTimes.org المفتوحة المصدر، ومكتبة Adhan (Batoul Apps) المتوفرة لـ Kotlin/Android كمرجع للتحقق من الحسابات.
- **نص القرآن الرقمي:** مشاريع مفتوحة مثل Tanzil.net وQuran.com كنقطة انطلاق، مع التحقق من دقة الرسم العثماني قبل الاعتماد النهائي.
- **بيانات الأحاديث:** مشاريع مفتوحة مثل Sunnah.com، مع التحقق من التخريج قبل الاعتماد.
- **خرائط المساجد:** OpenStreetMap لتفادي التبعية الكاملة لخدمات مدفوعة، مع إبقاء التوافق مع Google Maps كخيار.
- **توثيق Bubbles الرسمي:** صفحة `developer.android.com` الخاصة بـ Adaptive Apps / Bubbles لتنفيذ إشعار الأذكار العائم بشكل صحيح.

---

هذا المستند إطار عمل مرن لا قيد جامد: عدّله وأضف إليه كلما ظهرت حاجة جديدة للمستخدمين. الهدف النهائي أن يجد كل مسلم في هذا التطبيق أداة يومية موثوقة تُعينه على عباداته أينما كان.
