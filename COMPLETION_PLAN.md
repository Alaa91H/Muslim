# خطة استكمال تطبيق منارة (Manara) — وثيقة العمل الكاملة

> **تاريخ الفحص:** 15 أغسطس 2026 — **نوع الوثيقة:** خطة تنفيذ تشغيلية (تكمل `PROJECT_PROMPT.md` ولا تحلّ محلها)
> **كيف تقرأ هذه الوثيقة:** هي خارطة عمل مرتبة بالأولوية. كل بند يحمل: الهدف، الخطوات، معايير القبول، والاعتماديات. تُحدَّث علامات ✅ مع كل إنجاز فعلي.

---

## 0. ملخص تنفيذي

مشروع **منارة** في حالة ممتازة بنيويًا: 18 وحدة Gradle متعددة، بنية Clean Architecture مع Hilt، واجهة Compose كاملة، البناء **ناجح** و**61 اختبار وحدة ناجحًا بلا فشل**. لكن الإنجاز الفعلي للميزات يغطي **المرحلة 1 بالكامل تقريبًا + جزءًا من المرحلة 2 (القرآن)** فقط، بينما **7 وحدات feature فارغة** (أحاديث، أذكار، تسبيح، تعلّم، رمضان، زكاة، إعدادات) و**وحدتان أساسيتان فارغتان** (core-datastore، core-network).

**الخلاصة:** لا حاجة لإعادة بناء؛ الحاجة توزّع على 3 مسارات متوازية:

| المسار | الوصف | الأولوية |
|---|---|---|
| أ — تثبيت الأساس | وحدة الإعدادات، DataStore، سداد الديون التقنية، إتاحة الوصول | **P0** (قبل أي ميزة جديدة) |
| ب — إكمال المرحلتين 1 و2 | بقايا المرحلة 1 + إكمال القرآن صوتًا وصورة | **P0–P1** |
| ج — المراحل 3–8 | الأحاديث، الأذكار، التسبيح، التعلّم، رمضان، الزكاة، الإضافات | P1–P2 حسب التسلسل |

---

## 1. الوضع الحالي (نتيجة فحص موثّق)

### 1.1 التحقق الآلي الذي تم تنفيذه

| الفحص | النتيجة |
|---|---|
| `./gradlew :app:assembleDebug` | ✅ BUILD SUCCESSFUL |
| `./gradlew testDebugUnitTest` | ✅ 61 اختبارًا — 0 فشل، 0 أخطاء |
| `lintDebug` | ⏳ غير مشغّل محليًا (موجود في CI) |
| CI (GitHub Actions) | ✅ موجود: assemble + unit tests + lint لكل PR |

### 1.2 خريطة الوحدات الحالية

| الوحدة | الحالة | ملاحظات |
|---|---|---|
| `app` | ✅ تعمل | التنقّل الكامل في `ManaraApp.kt` — 5 تبويبات (الرئيسية، القرآن، المواقيت، القبلة، الإعدادات) + deep links `manara://` + اختصارات |
| `feature-prayer-times` (43 ملفًا) | ✅ مكتملة تقريبًا | المحرك الفلكي، الجدولة، الإشعارات، Widget، التصدير |
| `feature-qibla` (4 ملفات) | ✅ تعمل | بوصلة + حساسات + انحراف مغناطيسي — **تنقصها خريطة القبلة** |
| `feature-quran` (21 ملفًا) | 🔶 نصف مكتملة | نص كامل في Room + بحث FTS + علامات + آخر قراءة — **ينقصها الصوت والترجمات** |
| `feature-settings` / `feature-hadith` / `feature-adhkar` / `feature-tasbih` / `feature-learn` / `feature-ramadan` / `feature-zakat` | ⬜ **فارغة** (سكيلد فقط) | |
| `core-datastore` / `core-network` | ⬜ **فارغة** | معلنة في settings لكن بلا كود |
| `core-common` / `core-database` / `core-design-system` / `core-ui` / `core-location` / `core-notifications` | ✅ تعمل | |

### 1.3 فجوات بنيوية رصدها الفحص

1. **الإعدادات داخل وحدة خاطئة:** شاشة `PrayerSettingsScreen` تعيش في `feature-prayer-times` بينما المعمارية تقتضي `feature-settings` مستقلة (المحور العابر لكل المراحل).
2. **`core-datastore` بلا كود:** تعليق في `Theme.kt` يقول إن وضع الثيم (فاتح/داكن/تلقائي) سيُقرأ من DataStore «عندما تأتي وحدة الإعدادات» — البنية جاهزة والتنفيذ ناقص.
3. **ديون تقنية موثّقة:** تحذير `hiltViewModel` deprecated في 8 ملفات — يجب النقل إلى `androidx.hilt.lifecycle.viewmodel.compose`.
4. **لا ktlint ولا detekt** رغم ذكرهما في `PROJECT_PROMPT.md §3.8` (غير مضافين إلى `libs.versions.toml`).
5. **التدويل:** عربي + إنجليزي فقط (لا أردية/إندونيسية/تركية/فرنسية... ولا Weblate/Crowdin).
6. **القرآن:** لا أصوات قرّاء، لا ترجمات، لا تفاسير، لا تلوين تجويد، لا وضع ليلي للقارئ، لا متابعة ختمة.
7. **إطلاق الإصدار:** `isMinifyEnabled = false`، لا توقيع release، لا سياسة خصوصية في المستودع، `versionName = "0.1.0"`.

---

## 2. الأولويات والمنهجية

### 2.1 قواعد ملزمة (موروثة من وثيقة المشروع)

- لا تنتقل لمرحلة جديدة قبل أن تكتمل الحالية وتُختبَر على جهاز حقيقي.
- كل ميزة = وحدة feature مستقلة تعتمد على core فقط، **ولا تعتمد على feature أخرى**.
- المحتوى الديني (نص، أحاديث، أذكار، أحكام) يخضع لمراجعة شرعية منفصلة قبل الإصدار.
- الخصوصية: أذونات عند الحاجة فقط، بلا تتبّع.

### 2.2 ترتيب الأولويات

- **P0 — أساس لا يُبنى فوقه إلا بعد اكتماله:** وحدة الإعدادات، DataStore والثيم، سداد الديون التقنية، إتاحة الوصول، جودة الكود (lint نظيف + ktlint/detekt).
- **P1 — القيمة الدينية الجوهرية:** إكمال القرآن (صوت/ترجمة/تفسير)، الأحاديث، الأذكار والتسبيح، رمضان.
- **P2 — التوسع:** التعلّم، الزكاة، الميزات الإضافية، Wear OS، النسخ الاحتياطي.

---

## 3. خطة العمل التفصيلية

### المرحلة أ: تثبيت الأساس (P0) — «منظومة الإعدادات والجودة»

#### أ1. إنشاء وحدة `feature-settings` (أولوية قصوى)
- **الهدف:** مركز تحكم واحد لكل تفضيلات التطبيق (القسم «وحدة الإعدادات العامة» في وثيقة المشروع).
- **الخطوات:**
  1. نقل `PrayerSettingsScreen`/`PrayerSettingsViewModel` من `feature-prayer-times` إلى `feature-settings` (مع بقاء منطق الحساب في مكانه، والإعدادات تُقرأ عبر `core-datastore`).
  2. أقسام: الصلاة والأذان، القبلة، القرآن، المظهر (ثيم + حجم خط)، اللغة، الإشعارات، البيانات والخصوصية، حول التطبيق.
  3. ربط التبويب الخامس في `ManaraApp.kt` بوحدة الإعدادات الجديدة.
- **معايير القبول:** لا `import` من `feature-prayer-times` داخل `feature-settings`؛ كل قسم قابل للتوسع لاحقًا؛ الحالة محفوظة عبر DataStore.
- **الاعتماديات:** أ2 (DataStore).

#### أ2. تفعيل `core-datastore` (Preferences + Proto)
- **الهدف:** مصدر واحد لتفضيلات المستخدم (الموقع، طريقة الحساب، الثيم، حجم خط القرآن، لغة الواجهة...).
- **الخطوات:** إنشاء Preferences DataStore واحد + DataStore Proto للتفضيلات البنيوية (إن لزم)؛ هجرة تدريجية من `QuranPrefsRepository` الحالي.
- **معايير القبول:** كل ميزة تكتب/تقرأ تفضيلاتها عبر الوحدة؛ تدفق `Flow` غير قابل للتغيير؛ اختبارات وحدة لطبقة التخزين.
- **ملاحظة:** `PrayerSettingsRepository` الحالي يستخدم DataStore بالفعل داخل feature-prayer-times — يُوحَّد المسار.

#### أ3. سداد الديون التقنية
- نقل استدعاءات `hiltViewModel` إلى الحزمة الجديدة `androidx.hilt.lifecycle.viewmodel.compose` (8 ملفات).
- تفعيل R8/ProGuard للـ release بعد ضبط القواعد (اختبارات على جهاز حقيقي).
- مراجعة تحذيرات `lintDebug` حتى الصفر (تعمل حاليًا كعتبة في CI لكن بلا معالجة).

#### أ4. إتاحة الوصول والتخطيط التكيّفي
- **الهدف:** WCAG AA + TalkBack + دعم الشاشات الكبيرة (WindowSizeClass) كما تنص الوثيقة §4.
- **الخطوات:** جرد `contentDescription` لكل العناصر التفاعلية؛ التحقق من أهداف لمس ≥ 48dp؛ دعم Font Scale حتى 200%؛ تحويل الشريط السفلي إلى Navigation Rail على الشاشات العريضة؛ إيقاف الحركات حسب تفضيل النظام (`disableAnimations`).
- **معايير القبول:** فحص TalkBack يدوي لكل الشاشات الرئيسية؛ لا عنصر تفاعلي بلا تسمية؛ التخطيط لا ينكسر عند تكبير الخط.

#### أ5. جودة الكود والأتمتة
- إضافة ktlint + detekt إلى `libs.versions.toml` وربطهما بـ CI (عتبات صارمة: لا build ناجح مع مخالفات).
- رفع `lintDebug` إلى CI الحالي (موجود) + نشر تقارير الجودة كـ Artifacts.
- توثيق KDoc ناقص للوحدات العامة.

---

### المرحلة ب: إكمال المرحلة 1 (P0 — بقايا صغيرة)

| البند | الوصف | الحجم |
|---|---|---|
| ب1 — خريطة القبلة | عرض خط مستقيم من موقع المستخدم إلى الكعبة على خريطة (OSM أولًا حسب الوثيقة، أو Compose Map بديل بلا خادم) مع المسافة | M |
| ب2 — مكتبة أصوات الأذان | بنية `soundId` جاهزة بالفعل في النموذج — إضافة قائمة أصوات مدمجة + تنزيل اختياري لصوتيات إضافية عبر `core-network` (تفعيل الوحدة الفارغة) | M |
| ب3 — ربط الهجري برمضان | جاهزية الإعدادات (تعديل ±يوم) لتُستهلك تلقائيًا في تحديد بداية/نهاية رمضان والعيدين (تُستخدم في المرحلة ز) | S |

**معايير القبول:** الخريطة تعمل بلا إنترنت مع بيانات مدينة مدمجة؛ تغيير صوت الأذان لكل صلاة يسري فورًا؛ لا أذونات جديدة غير مبررة.

---

### المرحلة ج: إكمال المرحلة 2 — القرآن الكريم (P1)

ترتيب تنفيذي مقترح حسب الاعتماديات:

| # | البند | الوصف | الاعتماديات | الحجم |
|---|---|---|---|---|
| ج1 | وضع ليلي للقارئ + «قراءة مريحة» | ثيم داكن داخل القارئ + تباين أعلى وتباعد أسطر (الوثيقة §4.6) | — | S |
| ج2 | متابعة تقدّم الختمة | عدّاد آيات/أجزاء + رسم تقدم + خطة قراءة (يومية) | قاعدة البيانات الحالية | M |
| ج3 | ترجمات المعاني | نموذج `Translation` في Room (تُشحن أساسية عربية/إنجليزية + تنزيل اختياري) + عرض جنبًا إلى جنب | ج4 (البنية) | L |
| ج4 | تفاسير متعددة | ابن كثير، السعدي، الميسّر (تنزيل اختياري) + عرض مع الآية | — | L |
| ج5 | قرّاء متعددون + تنزيل | بيانات القارئ/السورة عبر `core-network`، تخزين في التخزين الداخلي مع إدارة مساحة يراها المستخدم | core-network | L |
| ج6 | تتبّع صوتي متزامن | تظليل الآية أثناء التلاوة (مزامنة MediaPlayer + مواقيت الآيات من بيانات التلاوة) | ج5 | XL |
| ج7 | وضع التكرار للحفظ | تكرار آية/مقطع بعدد مرات محدد + فاصل زمني | ج6 | M |
| ج8 | «آية اليوم» | إشعار اختياري (WorkManager + إشعارات) | core-notifications | S |
| ج9 | ترجمة كلمة بكلمة + تلوين التجويد | نموذج Word + بيانات تجويد (اختياري، مجتمعي لاحقًا) | — | L |

**معايير القبول:** القرآن يعمل 100% بلا إنترنت؛ التنزيلات قابلة للإدارة/الحذف؛ التزامن الصوتي دقيق (انزلاق < ثانيتين)؛ تعدد القرّاء يظهر في الإعدادات.

---

### المرحلة د: المرحلة 3 — الأحاديث (P1)

| البند | الوصف | الحجم |
|---|---|---|
| د1 | قاعدة بيانات Room مملوءة مسبقًا: الكتب الستة (بيانات الإسناد الأساسية) — من مصادر مفتوحة مع التحقق الشرعي | XL |
| د2 | تصنيف موضوعي + مجموعات مختصرة (الأربعون النووية، رياض الصالحين) | M |
| د3 | درجة الحديث (صحيح/حسن/ضعيف) مع الإشارة للمصدر | S |
| د4 | بحث FTS + «حديث اليوم» + علامات مرجعية + مشاركة نص/صورة | M |

**ملاحظة جوهرية:** حجم البيانات هنا كبير (آلاف الأحاديث) — يوصى ببناء **أداة توليد قاعدة بيانات** (import script) بدل الإدخال اليدوي، مع قناة مراجعة شرعية قبل الإصدار.

---

### المرحلة هـ: المرحلة 4 — الأذكار والتسبيح (P1)

| البند | الوصف | الحجم |
|---|---|---|
| هـ1 | مجموعات أذكار موثّقة المصدر (حصن المسلم أساسًا) + عدّاد تلقائي للتكرار | L |
| هـ2 | مسبحة إلكترونية: اهتزاز، هدف مخصص، سجل يومي/أسبوعي، إعادة ضبط | M |
| هـ3 | Widget مسبحة (Glance) | S |
| هـ4 | الإشعار العائم للأذكار: **Bubbles** (Android 11+) مع fallback لـ Heads-up — `core-notifications` هي موطن هذا الكود | XL |

---

### المرحلة و: المرحلة 5 — التعلّم (P2)

- دليل الوضوء/الغسل/التيمم خطوة بخطوة (نص + رسوم) — L
- دليل الصلوات الخمس التفاعلي + جدول الركعات + الفروقات المذهبية بحياد — L
- الصلوات الخاصة (جنازة، عيدين، تراويح، استخارة...) — M
- وضع مبسّط للأطفال — M

---

### المرحلة ز: المرحلة 6 — رمضان (P1–P2)

| البند | الاعتماديات | الحجم |
|---|---|---|
| ز1 — وضع رمضان: عدّاد سحور/إفطار + تنبيه سحور قابل للتخصيص | ب3 (الهجري) | M |
| ز2 — إشعار الإفطار مع دعاء الإفطار | core-notifications | S |
| ز3 — تقويم رمضان + متابعة أيام الصيام والقضاء | ب3 | L |
| ز4 — تنبيهات العشر الأواخر وليلة القدر | — | S |
| ز5 — تذكير زكاة الفطر | — | S |

---

### المرحلة ح: المرحلة 7 — حاسبة الزكاة (P2)

- زكاة المال (نقد، ذهب، فضة، عروض تجارة، استثمارات) بعد خصم الديون — L
- نصاب يُحدَّث عبر الشبكة مع بقاء الإدخال اليدوي (تفعيل `core-network`) — M
- زكاة الفطر + عملات متعددة + سجل عبر السنوات + شرح الأحكام — M

---

### المرحلة ط: المرحلة 8 — ميزات إضافية (P2)

- أسماء الله الحسنى (تفاعلي + شرح + صوت) — M
- مكتبة أدعية مصنّفة — M
- التقويم والمناسبات الإسلامية + تذكيرات مسبقة — M
- الصيام المسنون (اثنين/خميس، البيض، عرفة...) — S
- دليل الحج والعمرة — L
- باحث المساجد (OSM) — L
- ملفات متعددة للأسرة — M
- نسخ احتياطي/استعادة (ملف محلي + Drive اختياري) — M
- Wear OS — XL (مستقبلية)

---

### المرحلة ي: الجاهزية للإصدار (تُبدأ مبكرًا بالتوازي، وتُكتمل قبل الإطلاق)

| البند | الوصف |
|---|---|
| ي1 — سياسة خصوصية | ملف في المستودع + شاشة داخل التطبيق (الوثيقة §8) |
| ي2 — التوقيع والإصدار | تكوين release signing + R8/ProGuard + `versionName` مناسب |
| ي3 — التدويل | ربط Weblate/Crowdin + لغات أساسية جديدة (الأردية، الإندونيسية، التركية، الفرنسية...) |
| ي4 — اختبار الترجمة الزائفة | Pseudolocalization لضمان سلامة RTL والنصوص الطويلة |
| ي5 — تحسين الأداء | حجم APK، زمن الإقلاع < 2 ثانية، استهلاك البطارية (البوصلة تعمل فقط عند فتح الشاشة) |
| ي6 — مراجعة شرعية | نصوص القرآن مقابل مصحف مدني، الأحاديث/الأذكار من مصادر معتمدة (الوثيقة §10) |
| ي7 — إصلاح اسم الحزمة | `org.example.islamicapp` → حزمة نهائية عند التسجيل |

---

## 4. جدول زمني إرشادي (تقديري، بأيام عمل فرد واحد)

| المرحلة | الأيام التقديرية | ملاحظة |
|---|---|---|
| أ (الأساس) | 10–15 | غير قابلة للتأجيل |
| ب (بقايا المرحلة 1) | 5–8 | |
| ج (القرآن) | 25–40 | الأكبر؛ ج6 (المزامنة) أخطر بند تقنيًا |
| د (الأحاديث) | 15–25 | يعتمد على جاهزية البيانات + أداة الاستيراد |
| هـ (الأذكار والتسبيح) | 10–15 | |
| و (التعلّم) | 15–20 | |
| ز (رمضان) | 8–12 | |
| ح (الزكاة) | 8–12 | |
| ط (إضافات) | 15–25 | حسب الاختيار |
| ي (إصدار) | 5–10 | يبدأ بالتوازي |

**مسار حرج مقترح:** أ ← ب ← ج(1–4) ← ز ← ثم الباقي، مع بدء جمع ومراجعة بيانات الأحاديث مبكرًا لأنها الأبطأ.

---

## 5. معايير القبول العامة (لكل مرحلة)

1. البناء والاختبارات خضراء محليًا وفي CI (`assembleDebug` + `testDebugUnitTest` + `lintDebug` + ktlint/detekt).
2. اختبار يدوي على جهاز حقيقي (ميزات الخلفية: أذان، إشعارات، Widget) + اختبار على محاكي API 26 وAPI 37.
3. اختبارات وحدة جديدة للمنطق الخالص (كل محرك حسابي يُقارَن بمرجع موثوق).
4. الشاشات الجديدة: RTL/LTR، وضعان للثيم، TalkBack، تكبير خط 200%.
5. لا ميزة جديدة تعتمد على الشبكة بشكل إجباري (Offline-first).
6. تحديث `PROJECT_PROMPT.md` (علامات ✅) و`README.md` و`COMPLETION_PLAN.md` مع كل إنجاز.

---

## 6. المخاطر والتوصيات

| المخاطر | التوصية |
|---|---|
| **أذونات Android 13+ الدقيقة للتنبيهات** والقيود الجديدة على `SCHEDULE_EXACT_ALARM` | الاختبار على أجهزة حقيقية حديثة + بطاقة توجيه شفافة (موجودة جزئيًا) |
| **البيانات الدينية الضخمة** (أحاديث، تفاسير) — الإدخال اليدوي مستحيل | أداة استيراد/توليد قاعدة بيانات + أتمتة مراجعة |
| **المزامنة الصوتية** (أصعب بند تقنيًا في القرآن) | بروتوكول مواقيت آيات موحّد + اختبار على عدة تلاوات |
| **حجم APK** مع Room مملوءة مسبقًا | ضغط أصول (أو تنزيل عند الطلب للمحتوى الثقيل) + R8 |
| **ترخيص المحتوى** (نص القرآن من Tanzil، التلاوات، التفاسير) | توثيق كل مصدر في المستودع + الامتثال لشروط كل مصدر |
| **تأخير مراجعة المحتوى الشرعي** | بدء المراجعات بالتوازي مع التطوير، لا بعدها |
| **تفكك الإعدادات بين الوحدات** | فرض قاعدة: كل تفضيل يمر عبر `core-datastore`، و`feature-settings` هو الواجهة الوحيدة |

---

*هذه الوثيقة خطة تشغيلية؛ تبقى `PROJECT_PROMPT.md` مرجع الرؤية والبنية ومصدر الحقيقة النهائي. أي قرار معماري جديد يُسجَّل هناك أولًا.*
