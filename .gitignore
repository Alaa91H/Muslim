# Gradle
.gradle/
build/

# Local configuration (machine-specific)
local.properties

# IDE
.idea/
*.iml
.DS_Store
captures/
.externalNativeBuild/
.cxx/

# Android
*.apk
*.aab
*.keystore
!debug.keystore

# Kotlin
.kotlin/

# Freebuff local tool state
.freebuff/

# Local release signing (never commit)
keystore.properties
release.keystore

# Full hadith corpus — the compressed form ships in the repo (11 MB), the
# raw JSON is regenerated by scripts/import-hadith.py when re-importing.
feature/feature-hadith/src/main/assets/hadith_full.json
