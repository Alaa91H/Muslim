package org.muslim.app.core.common.appearance

/** Curated app-wide palette families with matched light and dark variants. */
enum class AppColorPalette {
    Classic,
    Emerald,
    Midnight,
    Sand,
    RoyalBlue,
    Turquoise,
    Olive,
    Burgundy,
    Amethyst,
    Copper,
    Slate,
    Sepia,
}

/** Controls the global Material card and control corner softness. */
enum class CardCornerStyle {
    Compact,
    Soft,
    Rounded,
}

/** Decorative Islamic motif shown in supported screen backgrounds. */
enum class AppOrnamentStyle {
    Geometry,
    Arabesque,
    Stars,
    Andalusian,
    Mashrabiya,
    Ottoman,
    Mushaf,
    Royal,
    Minimal,
}

/** Controls how prominent the selected Islamic ornament is across app surfaces. */
enum class OrnamentIntensity {
    Off,
    Subtle,
    Balanced,
    Rich,
}
