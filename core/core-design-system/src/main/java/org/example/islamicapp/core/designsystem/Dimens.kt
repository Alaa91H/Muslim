package org.example.islamicapp.core.designsystem

import androidx.compose.ui.unit.dp

/**
 * Shared spacing & shape tokens. Kept deliberately small — add tokens here
 * only when at least two modules need the same value.
 */
object ManaraSpacing {
    val XSmall = 4.dp
    val Small = 8.dp
    val Medium = 16.dp
    val Large = 24.dp
    val XLarge = 32.dp
}

/** Minimum touch target per accessibility guidance (48dp). */
object ManaraTouchTarget {
    val Min = 48.dp
}
